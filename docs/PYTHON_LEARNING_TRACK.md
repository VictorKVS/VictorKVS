# Python Learning Track

Older standalone Python repositories are kept as engineering-history evidence and are not treated as independent products.

## Verified learning repositories

- `shop_pit` — Python course homework; object-oriented programming and class design. Last visible commit: 2026-07-12.
- `usir-1` — homework on inheritance and method overriding with a user/session exercise. Last visible commit: 2026-07-13.
- `pit_ochibki` — error handling and custom exceptions exercise. Last visible commit: 2026-07-12.
- `brary_manager_project` — homework on modules, packages and `__init__.py`. Last visible commit: 2026-07-12.
- `files_serialization` — topic #12 homework covering filesystem work, encodings, JSON, archives, schema validation and logging. Last visible commit: 2026-07-12.
- `sait_pit` — empty historical placeholder; excluded from public product presentation.

## Public presentation

The useful progression is:

`OOP / class design → inheritance → exceptions → modules & packages → filesystem / serialization / backup`

The public site exposes this as one **Engineering Journey** layer. The underlying repositories remain unchanged so the learning history and commit provenance are preserved.
