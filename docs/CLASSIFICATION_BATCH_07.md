# Classification Batch 07 — residual learning/reference repositories

This batch follows the portfolio non-destructive policy. Inactivity older than three days is only a review signal. No repository is renamed, merged, archived or deleted by this classification.

| Repository | Evidence reviewed | Classification | Portfolio action |
|---|---|---|---|
| `Learn-Model-Context-Protocol-with-Python` | README identifies the repository as **Learn Model Context Protocol with Python, published by Packt**. Latest visible commits are from 2025-10-09 and include uploaded sample-application/setup material. | `LEARNING` | Preserve as MCP learning/reference material. Do not present it as an original VictorKVS product or as evidence of a shipped MCP platform. Keep outside Featured Engineering; reference only from the learning/journey layer if useful. |
| `TABL_MENDELEVA` | Latest visible commit is 2025-11-16. Repository contains a small Python application (`main.py`, `data.py`, templates) and a short project note. | `LEARNING` | Preserve as a bounded Python/web exercise. It can support the engineering journey but should not compete with product/MVP repositories. |
| `git_workouts` | Latest visible commit is 2024-04-13. Repository surface contains only `.gitignore` and a very small README. | `LEARNING` | Preserve as Git practice history; exclude from product/showcase foreground. |

## Result

These repositories belong to the historical learning/reference layer rather than the current product layer. They are intentionally retained for provenance and skill progression, but they do not affect flagship maturity claims.

## Protected boundaries

This batch does **not** modify or reclassify `PX00`, `KNOWLEDGE_CORE`, `KNOWLEDGE_MASTER`, active projects, or future knowledge-base boundaries.
